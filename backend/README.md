# Backend

This directory will contain the backend logic for the Society Registration app.
Currently, the app uses Firebase (client-side) for authentication and data storage.
Future backend implementations (e.g., Cloud Functions, Node.js server) should be placed here.
